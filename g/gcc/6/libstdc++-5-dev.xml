<?xml version="1.0" encoding="utf-8"?>
<package name="libstdc++-5-dev" section="libdevel">
    <description>
        <summary>GNU Standard C++ Library v3 (development files)</summary>
        <p>
        This package contains the headers and static library files necessary for
        building C++ programs which use libstdc++.
        </p>
        <p>
        libstdc++-v3 is a complete rewrite from the previous libstdc++-v2, which
        was included up to g++-2.95. The first version of libstdc++-v3 appeared
        in g++-3.0.
        </p>
    </description>

    <requires>
        <package name="gcc-5-base" version="= ${gcc:Version}"/>
        <package name="libgcc-5-dev" version="= ${gcc:Version}"/>
        <package name="libstdc++6" version="&gt;= ${gcc:Version}"/>
        <package name="${dep:libcdev}"/>
    </requires>

    <contents>
    </contents>
</package>
