<?xml version="1.0" encoding="utf-8"?>
<package name="python-libxslt1" section="python">
    <description>
        <summary>Python bindings for libxslt1</summary>
        <p>
        XSLT is an XML language for defining transformations of XML files from
        XML to some other arbitrary format, such as XML, HTML, plain text, etc.
        using standard XSLT stylesheets. libxslt is a C library which
        implements XSLT version 1.0.
        </p>
        <p>
        This package contains Python bindings for libxslt, needed to use
        libxslt in Python programs.
        </p>
    </description>

    <requires>
        <package name="python-libxml2"/>
    </requires>

    <contents>
        <file src="/usr/lib/python*/*-packages/*.py*"/>
        <file src="/usr/lib/python*/*-packages/*.so"/>
    </contents>
</package>
