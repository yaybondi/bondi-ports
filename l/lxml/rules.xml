<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[

true

    ]]>
    </prepare>

    <build>
    <![CDATA[

cd "$BOLT_SOURCE_DIR"

bh_python_install --py2 --build
bh_python_install --py3 --build

    ]]>
    </build>

    <install>
    <![CDATA[

cd "$BOLT_SOURCE_DIR"

install -d -m 0755 "$BOLT_INSTALL_DIR/python2"
install -d -m 0755 "$BOLT_INSTALL_DIR/python3"

bh_python_install --py2 --install "$BOLT_INSTALL_DIR/python2"
bh_python_install --py3 --install "$BOLT_INSTALL_DIR/python3"

cd "$BOLT_INSTALL_DIR"
find . -name "*.egg-info" -prune -exec rm -fr '{}' ';'

    ]]>
    </install>
</rules>
