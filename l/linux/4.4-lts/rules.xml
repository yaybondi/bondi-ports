<?xml version="1.0" encoding="utf-8"?>
<rules>

    <prepare>
    <![CDATA[

cd $BOLT_SOURCE_DIR
make mrproper

    ]]>
    </prepare>

    <build>
    <![CDATA[

cd $BOLT_SOURCE_DIR
make headers_check

    ]]>
    </build>

    <install>
    <![CDATA[

KERNEL_ARCH="`bh_kernel_arch_from_target_triplet $BOLT_HOST_TYPE`"

cd $BOLT_SOURCE_DIR
make INSTALL_HDR_PATH="$BOLT_INSTALL_DIR/$BOLT_INSTALL_PREFIX" \
    ARCH="$KERNEL_ARCH" headers_install

    ]]>
    </install>

</rules>
