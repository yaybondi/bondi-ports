<?xml version="1.0" encoding="utf-8"?>
<rules>

    <prepare>
    <![CDATA[

cd $BOLT_SOURCE_DIR
make mrproper

    ]]>
    </prepare>

    <build>
    <![CDATA[

cd $BOLT_SOURCE_DIR
make headers_check

    ]]>
    </build>

    <install>
    <![CDATA[

cd $BOLT_SOURCE_DIR

if [ "$BOLT_INSTALL_PREFIX" = "/tools" ]; then
    make INSTALL_HDR_PATH="$BOLT_INSTALL_DIR/$BOLT_INSTALL_PREFIX" \
        ARCH="x86" headers_install
else
    make INSTALL_HDR_PATH="$BOLT_INSTALL_DIR/$BOLT_INSTALL_PREFIX" \
        headers_install
fi

    ]]>
    </install>

</rules>
