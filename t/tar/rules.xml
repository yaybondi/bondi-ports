<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[

cd "$BOLT_SOURCE_DIR"
autoreconf -vfi

cd "$BOLT_BUILD_DIR"
$BOLT_SOURCE_DIR/configure \
    --prefix=/usr \
    --disable-nls \
    --libexecdir=/usr/lib/tar \
    --enable-backup-scripts \
    --with-lzma=xz \
    --disable-silent-rules

    ]]>
    </prepare>

    <build>
    <![CDATA[

cd "$BOLT_BUILD_DIR"
make -j"$BOLT_PARALLEL_JOBS"

    ]]>
    </build>

    <install>
    <![CDATA[

cd "$BOLT_BUILD_DIR"
make DESTDIR="$BOLT_INSTALL_DIR" install

mv "$BOLT_INSTALL_DIR/usr/lib/tar/rmt" "$BOLT_INSTALL_DIR/usr/sbin/rmt-tar"
mv "$BOLT_INSTALL_DIR/usr/sbin/backup" "$BOLT_INSTALL_DIR/usr/sbin/tar-backup"
mv "$BOLT_INSTALL_DIR/usr/sbin/restore" "$BOLT_INSTALL_DIR/usr/sbin/tar-restore"
mv "$BOLT_INSTALL_DIR/usr/lib/tar/backup.sh" "$BOLT_INSTALL_DIR/usr/lib/tar/"
mv "$BOLT_INSTALL_DIR/usr/lib/tar/dump-remind" "$BOLT_INSTALL_DIR/usr/lib/tar/"

    ]]>
    </install>
</rules>
