<?xml version="1.0" encoding="utf-8"?>
<package name="libssh2-1-dev" section="libdevel">
    <description>
        <summary>SSH2 client-side library (development headers)</summary>
        <p>
        libssh2 is a client-side C library implementing the SSH2 protocol.
        It supports regular terminal, SCP and SFTP (v1-v5) sessions;
        port forwarding, X11 forwarding; password, key-based and
        keyboard-interactive authentication.
        </p>
        <p>
        This package contains the development files.
        </p>
    </description>

    <requires>
        <package name="libssh2-1" version="=="/>
        <package name="libgcrypt20-dev"/>
    </requires>

    <contents>
        <file src="/usr/include/*"/>
        <file src="/usr/lib/*.a"/>
        <file src="/usr/lib/*.so"/>
        <file src="/usr/lib/pkgconfig/*.pc"/>
    </contents>
</package>
