<?xml version="1.0" encoding="utf-8"?>
<package name="libsepol1-dev" section="libdevel">
    <description>
        <summary>SELinux binary policy manipulation library and development files</summary>
        <p>
        libsepol allows programs to easily modify SELinux binary policies. This
        means changing the default values for booleans, or reading the policy for
        analysis.
        </p>
        <p>
        This package contains the headers and archives used for linking it into your
        programs.
        </p>
    </description>

    <requires>
        <package name="libsepol1" version="=="/>
    </requires>

    <contents>
        <file src="${prefix}/include/sepol"/>
        <file src="${prefix}/lib/libsepol.a"/>
        <file src="${prefix}/lib/libsepol.so"/>
        <file src="${prefix}/lib/pkgconfig"/>
    </contents>
</package>
