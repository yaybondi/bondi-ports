<?xml version="1.0" encoding="utf-8"?>
<package name="perl-base" section="perl">
    <description>
        <summary>minimal Perl system</summary>
        <p>
        Perl is a scripting language used in many system scripts and utilities.
        </p>
        <p>
        This package is not separately maintained on Bolt OS. It exists only
        for the sake of keeping package naming compatibility with Debian. All
        modules are in packages libperl and perl-modules.
        </p>
        <p>
        This package contains the Perl interpreter without any modules.
        </p>
    </description>

    <contents>
        <dir src="${prefix}/lib/perl"/>
        <file src="${prefix}/bin/perl"/>
        <file src="${prefix}/bin/perl5*"/>
    </contents>
</package>
