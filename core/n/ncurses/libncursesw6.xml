<?xml version="1.0" encoding="utf-8"?>
<package name="libncursesw6" section="libs">
    <description>
        <summary>Shared libraries for terminal handling (wide character support)</summary>
        <p>
        The ncurses library routines are a terminal-independent method of
        updating character screens with reasonable optimization.
        </p>
        <p>
        This package contains the shared libraries necessary to run programs
        compiled with ncursesw, which includes support for wide characters.
        </p>
    </description>

    <requires>
        <package name="libtinfo6" version="=="/>
    </requires>

    <contents>
        <file src="${prefix}/lib/libformw.so.6*"/>
        <file src="${prefix}/lib/libmenuw.so.6*"/>
        <file src="${prefix}/lib/libncursesw.so.6*"/>
        <file src="${prefix}/lib/libpanelw.so.6*"/>
    </contents>
</package>
