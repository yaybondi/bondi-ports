<?xml version="1.0" encoding="utf-8"?>
<package name="libffi7" section="libs">
    <description>
        <summary>Foreign Function Interface library runtime</summary>
        <p>
        A foreign function interface is the popular name for the interface that
        allows code written in one language to call code written in another
        language.
        </p>
    </description>

    <contents>
        <file src="${prefix}/lib/libffi.so.*"/>
    </contents>
</package>
