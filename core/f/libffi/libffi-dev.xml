<?xml version="1.0" encoding="utf-8"?>
<package name="libffi-dev" section="libdevel">
    <description>
        <summary>Foreign Function Interface library (development files)</summary>
        <p>
        This package contains the headers and static library files necessary for
        building programs which use libffi.
        </p>
        <p>
        A foreign function interface is the popular name for the interface that
        allows code written in one language to call code written in another
        language.
        </p>
    </description>

    <requires>
        <package name="libffi7" version="=="/>
    </requires>

    <contents>
        <file src="${prefix}/include"/>
        <file src="${prefix}/lib/libffi.a"/>
        <file src="${prefix}/lib/libffi.so"/>
        <file src="${prefix}/lib/libffi_pic.a"/>
        <file src="${prefix}/lib/pkgconfig"/>
    </contents>
</package>
