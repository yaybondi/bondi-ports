<?xml version="1.0" encoding="utf-8"?>
<package name="libprocps-dev" section="libdevel">
    <description>
        <summary>Library for accessing process information from /proc</summary>
        <p>
        These are the header files for libproc. Some packages using libproc
        to access process information from /proc need these to compile.
        </p>
    </description>

    <requires>
        <package name="libprocps8" version="=="/>
    </requires>

    <contents subdir="procps">
        <file src="${prefix}/include"/>
        <file src="${prefix}/lib/libprocps.a"/>
        <file src="${prefix}/lib/libprocps.so"/>
        <file src="${prefix}/lib/pkgconfig"/>
    </contents>
</package>
