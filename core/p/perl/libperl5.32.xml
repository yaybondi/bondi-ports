<?xml version="1.0" encoding="utf-8"?>
<package name="libperl5.32" section="libs">
    <description>
        <summary>shared Perl library</summary>
        <p>
        This package contains the shared Perl library, used by applications
        which embed a Perl interpreter.
        </p>
        <p>
        It also contains the architecture-dependent parts of the standard
        library (and depends on perl-modules-5.32 which contains the
        architecture-independent parts).
        </p>
    </description>

    <requires>
        <package name="perl-modules-5.32" version=">=="/>
    </requires>

    <contents>
        <file src="${prefix}/lib/libperl.so.*"/>
        <file src="${prefix}/lib/perl"/>
    </contents>
</package>
