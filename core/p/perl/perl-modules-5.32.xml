<?xml version="1.0" encoding="utf-8"?>
<package name="perl-modules-5.32" section="perl">
    <description>
        <summary>Core Perl modules</summary>
        <p>
        Architecture independent Perl modules. These modules are part of Perl and
        required if the `perl' package is installed.
        </p>
        <p>
        Note that this package only exists to save archive space and should be
        considered an internal implementation detail of the `perl' package.
        Other packages should not depend on `perl-modules-5.32' directly, they
        should use `perl' (which depends on `perl-modules-5.32') instead.
        </p>
    </description>

    <contents>
        <file src="${prefix}/share/perl"/>
    </contents>
</package>
