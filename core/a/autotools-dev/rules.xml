<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[

true

    ]]>
    </prepare>

    <build>
    <![CDATA[

true

    ]]>
    </build>

    <install>
    <![CDATA[

cd "$BONDI_BUILD_DIR"
make DESTDIR="$BONDI_INSTALL_DIR" PREFIX="$BONDI_INSTALL_PREFIX" install

    ]]>
    </install>
</rules>
