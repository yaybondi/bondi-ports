<?xml version="1.0" encoding="utf-8"?>
<package name="libassuan-dev" section="libdevel">
    <description>
        <summary>IPC library for the GnuPG components -- header files</summary>
        <p>
        Libassuan is a small library implementing the so-called "Assuan
        protocol". This protocol is used for IPC between most newer GnuPG
        components. Both server and client side functions are provided.
        </p>
        <p>
        This package contains header files and static libraries for libassuan.
        </p>
    </description>

    <requires>
        <package name="libassuan0" version="=="/>
    </requires>

    <contents>
        <file src="${prefix}/bin"/>
        <file src="${prefix}/include"/>
        <file src="${prefix}/lib/libassuan.a"/>
        <file src="${prefix}/lib/libassuan.so"/>
        <file src="${prefix}/share/aclocal"/>
    </contents>
</package>
