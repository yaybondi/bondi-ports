<?xml version="1.0" encoding="utf-8"?>
<package name="guile-2.2-dev" section="lisp">
    <description>
        <summary>Development files for Guile 2.2</summary>
        <p>
        This package contains files needed for development using Guile 2.2.
        </p>
        <p>
        Guile is a Scheme implementation designed for real world programming,
        providing a rich Unix interface, a module system, an interpreter, and
        many extension languages. Guile can be used as a standard #! style
        interpreter, via #!${prefix}/bin/guile, or as an extension language for
        other applications via libguile.
        </p>
    </description>

    <requires>
        <package name="guile-2.2" version="=="/>
        <package name="guile-2.2-libs" version="=="/>
        <package name="libc-dev"/>
        <package name="libncurses-dev"/>
        <package name="libreadline-dev"/>
        <package name="libltdl-dev"/>
        <package name="libgmp-dev"/>
        <package name="libgc-dev"/>
        <package name="pkg-config"/>
    </requires>

    <contents>
        <file src="${prefix}/bin/guild"/>
        <file src="${prefix}/bin/guile-config"/>
        <file src="${prefix}/bin/guile-snarf"/>
        <file src="${prefix}/bin/guile-tools"/>
        <file src="${prefix}/include/guile/2.2"/>
        <file src="${prefix}/lib/libguile-*.a"/>
        <file src="${prefix}/lib/libguile-*.so"/>
        <file src="${prefix}/lib/libguilereadline-*.a"/>
        <file src="${prefix}/lib/pkgconfig"/>
        <file src="${prefix}/share/aclocal"/>
        <file src="${prefix}/share/guile/2.2/scripts"/>
    </contents>
</package>
