<?xml version="1.0" encoding="utf-8"?>
<package name="locales" section="localization">
    <description>
        <summary>GNU C Library: National Language (locale) data [support]</summary>
        <p>
        Machine-readable data files, shared objects and programs used by the
        C library for localization (l10n) and internationalization (i18n) support.
        </p>
        <p>
        This package contains tools to generate locale definitions from source
        files (included in this package). It allows you to customize which
        definitions actually get generated. This is a space-saver over how this
        package used to be, with all locales generated by default. This created
        a package that unpacked to an excess of 30 megs.
        </p>
    </description>

    <requires>
        <package name="libc-bin" version=">> 2.31"/>
    </requires>

    <contents>
        <file src="/etc/locale.alias"/>

        <file src="/usr/sbin/locale-gen"/>
        <file src="/usr/sbin/update-locale"/>
        <file src="/usr/sbin/validlocale"/>

        <file src="/usr/share/i18n"/>

        <dir src="/usr/lib/locale"/>
    </contents>
</package>
