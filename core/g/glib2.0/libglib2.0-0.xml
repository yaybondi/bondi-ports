<?xml version="1.0" encoding="utf-8"?>
<package name="libglib2.0-0" section="libs">
    <description>
        <summary>GLib library of C routines</summary>
        <p>
        GLib is a library containing many useful C routines for things such
        as trees, hashes, lists, and strings. It is a useful general-purpose
        C library used by projects such as GTK+, GIMP, and GNOME.
        </p>
        <p>
        This package contains the shared libraries.
        </p>
    </description>

    <contents>
        <dir src="${prefix}/lib/gio"/>
        <dir src="${prefix}/lib/gio/modules"/>
        <dir src="${prefix}/lib/glib-2.0"/>
        <file src="${prefix}/lib/libgio-2.0.so.*"/>
        <file src="${prefix}/lib/libglib-2.0.so.*"/>
        <file src="${prefix}/lib/libgmodule-2.0.so.*"/>
        <file src="${prefix}/lib/libgobject-2.0.so.*"/>
        <file src="${prefix}/lib/libgthread-2.0.so.*"/>
        <dir src="${prefix}/share/glib-2.0"/>
        <dir src="${prefix}/share/glib-2.0/schemas"/>
    </contents>
</package>
