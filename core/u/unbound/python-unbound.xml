<?xml version="1.0" encoding="utf-8"?>
<package name="python-unbound" section="python">
    <description>
        <summary>library implementing DNS resolution and validation (Python bindings)</summary>
        <p>
        Python extension module for libunbound.
        </p>
        <p>
        libunbound performs and validates DNS lookups; it can be used to convert
        hostnames to IP addresses and back and obtain other information from the
        DNS. Cryptographic validation of results is performed with DNSSEC.
        </p>
    </description>

    <requires>
        <package name="python"/>
    </requires>

    <contents collect-py-cache-files="true">
        <file src="${prefix}/lib/python2.7/dist-packages"/>
    </contents>
</package>
