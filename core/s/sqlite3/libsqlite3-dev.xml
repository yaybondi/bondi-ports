<?xml version="1.0" encoding="utf-8"?>
<package name="libsqlite3-dev" section="libdevel">
    <description>
        <summary>SQLite 3 development files</summary>
        <p>
        SQLite is a C library that implements an SQL database engine. 
        Programs that link with the SQLite library can have SQL database 
        access without running a separate RDBMS process.
        </p>
        <p>
        This package contains the development files (headers, static libraries)
        </p>
    </description>

    <requires>
        <package name="libsqlite3-0" version="=="/>
    </requires>

    <contents>
        <file src="${prefix}/include"/>
        <file src="${prefix}/lib/libsqlite3.a"/>
        <file src="${prefix}/lib/libsqlite3.so"/>
        <file src="${prefix}/lib/pkgconfig"/>
    </contents>
</package>
