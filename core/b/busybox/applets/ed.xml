<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-ed-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the ed package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>ed</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="ed"/>
    </conflicts>

    <replaces>
        <package name="ed"/>
    </replaces>

    <contents>
        <file src="${prefix}/bin/ed"/>
    </contents>
</package>
