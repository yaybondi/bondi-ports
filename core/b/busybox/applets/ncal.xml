<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-ncal-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the ncal package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>cal</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="ncal"/>
    </conflicts>

    <replaces>
        <package name="ncal"/>
    </replaces>

    <contents>
        <file src="/usr/bin/cal"/>
    </contents>
</package>
