<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-loadlin-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the loadlin package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>freeramdisk</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="loadlin"/>
    </conflicts>

    <replaces>
        <package name="loadlin"/>
    </replaces>

    <contents>
        <file src="${prefix}/sbin/freeramdisk"/>
    </contents>
</package>
