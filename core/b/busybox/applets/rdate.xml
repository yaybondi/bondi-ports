<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-rdate-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the rdate package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>rdate</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="rdate"/>
    </conflicts>

    <contents>
        <file src="${prefix}/sbin/rdate"/>
    </contents>
</package>
