<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-dash-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the dash package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>sh</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="dash"/>
    </conflicts>

    <contents>
        <file src="${prefix}/bin/sh"/>
    </contents>
</package>
