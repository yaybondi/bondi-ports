<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-fdflush-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the fdflush package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>fdflush</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="fdflush"/>
    </conflicts>

    <replaces>
        <package name="fdflush"/>
    </replaces>

    <contents>
        <file src="/usr/bin/fdflush"/>
    </contents>
</package>
