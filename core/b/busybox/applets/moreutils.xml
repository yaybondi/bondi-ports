<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-moreutils-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the moreutils package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>ts</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="moreutils"/>
    </conflicts>

    <replaces>
        <package name="moreutils"/>
    </replaces>

    <contents>
        <file src="${prefix}/bin/ts"/>
    </contents>
</package>
