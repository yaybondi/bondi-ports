<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-patch-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the patch package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>patch</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="patch"/>
    </conflicts>

    <replaces>
        <package name="patch"/>
    </replaces>

    <contents>
        <file src="/usr/bin/patch"/>
    </contents>
</package>
