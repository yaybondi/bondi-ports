<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-pscan-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the pscan package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>pscan</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="pscan"/>
    </conflicts>

    <replaces>
        <package name="pscan"/>
    </replaces>

    <contents>
        <file src="${prefix}/bin/pscan"/>
    </contents>
</package>
