<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-usbutils-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the usbutils package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>lsusb</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="usbutils"/>
    </conflicts>

    <replaces>
        <package name="usbutils"/>
    </replaces>

    <contents>
        <file src="${prefix}/bin/lsusb"/>
    </contents>
</package>
