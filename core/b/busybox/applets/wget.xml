<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-wget-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the wget package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>wget</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="wget"/>
    </conflicts>

    <replaces>
        <package name="wget"/>
    </replaces>

    <contents>
        <file src="/usr/bin/wget"/>
    </contents>
</package>
