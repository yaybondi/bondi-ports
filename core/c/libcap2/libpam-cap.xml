<?xml version="1.0" encoding="utf-8"?>
<package name="libpam-cap" section="admin">
    <description>
        <summary>POSIX 1003.1e capabilities (PAM module)</summary>
        <p>
        Libcap implements the user-space interfaces to the POSIX 1003.1e capabilities
        available in Linux kernels. These capabilities are a partitioning of the all
        powerful root privilege into a set of distinct privileges.
        </p>
        <p>
        This package contains the PAM module for enforcing capabilities on users and
        groups at PAM session start time.
        </p>
    </description>

    <requires>
        <package name="libpam-runtime" version=">= 1.1.3-2~"/>
    </requires>

    <contents>
        <file src="${prefix}/lib/security"/>
    </contents>
</package>
