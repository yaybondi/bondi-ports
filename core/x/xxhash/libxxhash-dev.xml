<?xml version="1.0" encoding="utf-8"?>
<package name="libxxhash-dev" section="libdevel">
    <description>
        <summary>header files and a static library for libxxhash</summary>
        <p>
        xxHash is an Extremely fast Hash algorithm, running at RAM speed limits.
        It successfully completes the SMHasher test suite which evaluates collision,
        dispersion and randomness qualities of hash functions. Code is highly portable,
        and hashes are identical on all platforms (little / big endian).
        </p>
        <p>
        This package contains xxHash static library and header files.
        </p>
    </description>

    <requires>
        <package name="libxxhash0" version="=="/>
    </requires>

    <contents>
        <file src="/usr/include"/>
        <file src="/usr/lib/libxxhash.{a,so}"/>
        <file src="/usr/lib/pkgconfig"/>
    </contents>
</package>
