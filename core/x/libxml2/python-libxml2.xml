<?xml version="1.0" encoding="utf-8"?>
<package name="python-libxml2" section="python">
    <description>
        <summary>Python bindings for the GNOME XML library</summary>
        <p>
        XML is a metalanguage to let you design your own markup language.
        A regular markup language defines a way to describe information in
        a certain class of documents (eg HTML). XML lets you define your
        own customized markup languages for many classes of document. It
        can do this because it's written in SGML, the international standard
        metalanguage for markup languages.
        </p>
        <p>
        This package contains the files needed to use the GNOME XML library
        in Python programs.
        </p>
    </description>

    <requires>
        <package name="python"/>
    </requires>

    <contents collect-py-cache-files="true">
        <file src="${prefix}/lib/python2*/*-packages/*.py*"/>
        <file src="${prefix}/lib/python2*/*-packages/*.so"/>
    </contents>
</package>
