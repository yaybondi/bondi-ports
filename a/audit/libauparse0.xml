<?xml version="1.0" encoding="utf-8"?>
<package name="libauparse0" section="libs">
    <description>
        <summary>Dynamic library for parsing security auditing</summary>
        <p>
        The libauparse package contains the dynamic libraries needed for
        applications to use the audit framework. It is used to monitor systems for
        security related events.
        </p>
        <p>
        This package contains the libauparse0 library.
        </p>
    </description>

    <contents>
        <file src="${prefix}/lib/libauparse.so.*"/>
    </contents>
</package>
