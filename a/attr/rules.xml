<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[

cd "$BOLT_SOURCE_DIR"
autoreconf -vfi

cd "$BOLT_BUILD_DIR"
$BOLT_SOURCE_DIR/configure \
    --prefix="$BOLT_INSTALL_DIR/usr" \
    --enable-gettext=no

# why does this package insist on using gettext?
echo "
#define _(x) x
#define bindtextdomain(x, y)
#define textdomain(x)
" >> include/config.h

    ]]>
    </prepare>

    <build>
    <![CDATA[

cd "$BOLT_BUILD_DIR"
make -j"$BOLT_PARALLEL_JOBS"

    ]]>
    </build>

    <install>
    <![CDATA[

cd "$BOLT_BUILD_DIR"
make install
make install-dev
make install-lib

    ]]>
    </install>
</rules>
