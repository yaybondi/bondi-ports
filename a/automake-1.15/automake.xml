<?xml version="1.0" encoding="utf-8"?>
<package name="automake" section="devel">
    <description>
        <summary>Tool for generating GNU Standards-compliant Makefiles</summary>
        <p>
        Automake is a tool for automatically generating `Makefile.in's from
        files called `Makefile.am'.
        </p>
        <p>
        The goal of Automake is to remove the burden of Makefile maintenance
        from the back of the individual GNU maintainer (and put it on the back
        of the Automake maintainer).
        </p>
        <p>
        The `Makefile.am' is basically a series of `make' macro definitions
        (with rules being thrown in occasionally). The generated
        `Makefile.in's are compliant with the GNU Makefile standards.
        </p>
        <p>
        Automake 1.15 fails to work in a number of situations that Automake
        1.4, 1.6, 1.7, 1.8, 1.9, 1.10, 1.11 and 1.14 did, so previous versions are
        available as separate packages.
        </p>
    </description>

    <requires>
        <package name="autoconf" version=">= 2.65"/>
        <package name="autotools-dev" version=">= 20020320.1"/>
    </requires>

    <contents>
        <file src="${prefix}/bin"/>
        <dir src="${prefix}/share/aclocal"/>
        <file src="${prefix}/share/aclocal-1.15"/>
        <file src="${prefix}/share/automake-1.15"/>
    </contents>
</package>
