<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-sed-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the sed package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>sed</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <provides>
        <package name="sed"/>
    </provides>

    <conflicts>
        <package name="sed"/>
    </conflicts>

    <replaces>
        <package name="sed"/>
    </replaces>

    <contents>
        <file src="${prefix}/bin/sed"/>
    </contents>
</package>
