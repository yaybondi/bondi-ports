<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-ipcalc-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the ipcalc package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>ipcalc</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="ipcalc"/>
    </conflicts>

    <contents>
        <file src="${prefix}/bin/ipcalc"/>
    </contents>
</package>
