<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-less-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the less package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>less</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <provides>
        <package name="less"/>
    </provides>

    <conflicts>
        <package name="less"/>
    </conflicts>

    <replaces>
        <package name="less"/>
    </replaces>

    <contents>
        <file src="${prefix}/bin/less"/>
    </contents>
</package>
