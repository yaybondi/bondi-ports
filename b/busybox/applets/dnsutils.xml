<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-dnsutils-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the dnsutils package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>nslookup</li>
        </ul>
    </description>

    <requires>
        <package name="base-files"/>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="dnsutils"/>
    </conflicts>

    <replaces>
        <package name="dnsutils"/>
    </replaces>

    <contents>
        <file src="${prefix}/bin/nslookup"/>
    </contents>
</package>
