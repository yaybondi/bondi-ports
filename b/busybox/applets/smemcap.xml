<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-smemcap-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the smemcap package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>smemcap</li>
        </ul>
    </description>

    <requires>
        <package name="base-files"/>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="smemcap"/>
    </conflicts>

    <replaces>
        <package name="smemcap"/>
    </replaces>

    <contents>
        <file src="/usr/bin/smemcap"/>
    </contents>
</package>
