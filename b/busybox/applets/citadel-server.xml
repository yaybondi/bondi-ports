<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-citadel-server-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the citadel-server package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>sendmail</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="citadel-server"/>
    </conflicts>

    <replaces>
        <package name="citadel-server"/>
    </replaces>

    <contents>
        <file src="${prefix}/sbin/sendmail"/>
    </contents>
</package>
