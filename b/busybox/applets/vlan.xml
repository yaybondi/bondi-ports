<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-vlan-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the vlan package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>vconfig</li>
        </ul>
    </description>

    <requires>
        <package name="base-files"/>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="vlan"/>
    </conflicts>

    <replaces>
        <package name="vlan"/>
    </replaces>

    <contents>
        <file src="/usr/sbin/vconfig"/>
    </contents>
</package>
