<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-bc-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the bc package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>bc</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="bc"/>
    </conflicts>

    <replaces>
        <package name="bc"/>
    </replaces>

    <contents>
        <file src="${prefix}/bin/bc"/>
    </contents>
</package>
