<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-setserial-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the setserial package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>setserial</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <provides>
        <package name="setserial"/>
    </provides>

    <conflicts>
        <package name="setserial"/>
    </conflicts>

    <replaces>
        <package name="setserial"/>
    </replaces>

    <contents>
        <file src="${prefix}/bin/setserial"/>
    </contents>
</package>
