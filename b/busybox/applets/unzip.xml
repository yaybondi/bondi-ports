<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-unzip-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the unzip package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>unzip</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="unzip"/>
    </conflicts>

    <contents>
        <file src="${prefix}/bin/unzip"/>
    </contents>
</package>
