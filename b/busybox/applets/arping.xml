<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-arping-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the arping package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>arping</li>
        </ul>
    </description>

    <requires>
        <package name="base-files"/>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="arping"/>
    </conflicts>

    <replaces>
        <package name="arping"/>
    </replaces>

    <contents>
        <file src="/usr/sbin/arping"/>
    </contents>
</package>
