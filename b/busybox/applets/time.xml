<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-time-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the time package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>time</li>
        </ul>
    </description>

    <requires>
        <package name="base-files"/>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="time"/>
    </conflicts>

    <replaces>
        <package name="time"/>
    </replaces>

    <contents>
        <file src="${prefix}/bin/time"/>
    </contents>
</package>
