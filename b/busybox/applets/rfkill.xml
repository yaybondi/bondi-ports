<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-rfkill-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the rfkill package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>rfkill</li>
        </ul>
    </description>

    <requires>
        <package name="base-files"/>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="rfkill"/>
    </conflicts>

    <replaces>
        <package name="rfkill"/>
    </replaces>

    <contents>
        <file src="${prefix}/sbin/rfkill"/>
    </contents>
</package>
