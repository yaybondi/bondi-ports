<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-acpid-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the acpid package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>acpid</li>
        </ul>
    </description>

    <requires>
        <package name="busybox"/>
    </requires>

    <provides>
        <package name="acpid"/>
    </provides>

    <conflicts>
        <package name="acpid"/>
    </conflicts>

    <replaces>
        <package name="acpid"/>
    </replaces>

    <contents>
        <file src="${prefix}/sbin/acpid"/>
    </contents>
</package>
