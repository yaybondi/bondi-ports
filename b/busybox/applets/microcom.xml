<?xml version="1.0" encoding="utf-8"?>
<package name="busybox-microcom-symlinks" section="admin">
    <description>
        <summary>Substitutes for (some) programs from the microcom package</summary>
        <p>
            This package contains the symbolic links for the following applets:
        </p>
        <ul>
            <li>microcom</li>
        </ul>
    </description>

    <requires>
        <package name="base-files"/>
        <package name="busybox"/>
    </requires>

    <conflicts>
        <package name="microcom"/>
    </conflicts>

    <replaces>
        <package name="microcom"/>
    </replaces>

    <contents>
        <file src="${prefix}/bin/microcom"/>
    </contents>
</package>
