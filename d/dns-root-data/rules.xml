<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[

true

    ]]>
    </prepare>

    <build>
    <![CDATA[

true

    ]]>
    </build>

    <install>
    <![CDATA[

cp -a "$BOLT_BUILD_DIR/"* "$BOLT_INSTALL_DIR/"

if [ "$BOLT_BUILD_FOR" = "tools" ]; then
    mv "$BOLT_INSTALL_DIR/usr" "$BOLT_INSTALL_DIR/tools"
fi

    ]]>
    </install>
</rules>
