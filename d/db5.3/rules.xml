<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[

cd "$BOLT_BUILD_DIR/build_unix"
../dist/configure \
    --prefix=/usr \
    --localstatedir=/var \
    --sysconfdir=/etc \
    --libexecdir=/usr/lib \
    --enable-cxx \
    --enable-compat185 \
    --enable-sql \
    --enable-stl \
    --enable-dbm

    ]]>
    </prepare>

    <build>
    <![CDATA[

cd "$BOLT_BUILD_DIR/build_unix"
make -j"$BOLT_PARALLEL_JOBS"

    ]]>
    </build>

    <install>
    <![CDATA[

cd "$BOLT_BUILD_DIR/build_unix"
make DESTDIR="$BOLT_INSTALL_DIR" install

cd "$BOLT_INSTALL_DIR/usr/bin"
for prog in db_*; do
    newname=`echo $prog | sed 's/db_/db5.3_/g'`
    mv $prog $newname
done
mv dbsql db5.3_sql

    ]]>
    </install>
</rules>
